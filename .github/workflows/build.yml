name: Build and Release

on:
  push:
    branches:
      - prepare_release
    tags:
      - 'v*.*.*'

jobs:
  build:
    name: Build
    strategy:
      matrix:
        os:
          - linux
          - windows
          - macos
        python-version:
          - '11'
        include:
          - os: linux
            platform: linux
            runs-on: ubuntu-latest

          - os: windows
            platform: windows
            runs-on: windows-latest

          - os: macos
            platform: darwin
            runs-on: macos-latest
            
    runs-on: ${{ matrix.runs-on }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Create Virtual Environment
        working-directory: './houdini/TumblePipe/python3.${{ matrix.python-version }}libs/external/'
        run: uv venv ${{ matrix.platform }} --python 3.${{ matrix.python-version }}
      
      - name: Install Dependencies
        working-directory: './houdini/TumblePipe/python3.${{ matrix.python-version }}libs/external/${{ matrix.platform }}'
        run: uv pip install --no-cache-dir --upgrade -r ../requirements.txt

      - name: Build Zip
        uses: byteever/action-build-zip@v1
        with:
          filename: TumblePipe_${{ matrix.platform }}_py3${{ matrix.python-version }}.zip
      
      - name: Upload Artifact
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform }}-py3${{ matrix.python-version }}
          path: dist/TumblePipe_${{ matrix.platform }}_py3${{ matrix.python-version }}.zip
  
  release:
    name: Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:

      - name: Download Artifacts
        uses: actions/download-artifact@v4

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref }}
          files: '*.zip'